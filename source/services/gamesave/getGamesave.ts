import type { Context } from "../global.types";
import { base } from "../../config/constants";
import axios from "axios";

export const getGamesave = async (ctx: Context): Promise<void> => {
  console.log(ctx);
};
